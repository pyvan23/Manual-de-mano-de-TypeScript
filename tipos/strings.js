"use strict";
(() => {
    var _a;
    const batman = 'Batman';
    const linternaVerde = 'Linterna verde';
    const volcan = `Heroe: ${batman} volcan negro`;
    console.log(volcan.toLowerCase());
    console.log(((_a = batman[10]) === null || _a === void 0 ? void 0 : _a.toLocaleUpperCase()) || 'es undefined');
})();
